# TODO веб-приложения

Этот документ фиксирует задачи именно по веб-части системы: frontend,
web API, UX, сохранение анкеты, авторизация, роли и интерфейсы пользователей.

Задачи profile-agent, LLM-промптов, генерации профилей и сравнения вынесены в
отдельный будущий трек. В этом документе они упоминаются только как точки
интеграции, которые веб-приложение должно уметь показать пользователю.

## 1. Контракт ответов анкеты на стороне web

- Добавлено:
  - TypeScript/Zod-схемы в `apps/web/src/schemas/answerPayload.ts`;
  - `DraftAnswerPayload` - permissive-схема для частично заполненного
    черновика;
  - `SubmittedAnswerPayload` - strict-схема для финальной отправки;
  - генерация JSON Schema командой `bun run generate:answer-schema` из
    `apps/web`;
  - структурная проверка draft/submit в web API;
  - отдельная проверка бизнес-полноты анкеты по `questionnaire.ts` при submit.
  - базовая проверка совместимости контрактов командой
    `bun run check:answer-schema`.
- Оставшиеся задачи:
  - расширить схему структурированными ids выбранной должности/оргструктуры,
    когда они появятся в payload;
  - синхронизировать будущие Pydantic-модели profile-agent с этой Zod/JSON
    Schema.
- Добавить frontend/backend тесты на:
  - валидный draft payload;
  - невалидный draft payload;
  - валидный submitted payload;
  - submitted payload с незаполненными обязательными блоками.
- Согласовать с Python-треком, что Pydantic-модели profile-agent зеркалят
  Zod/JSON Schema, но реализация Pydantic относится не к web backlog.

## 2. Сохранение анкеты в PostgreSQL

- Добавлен переходный DB-путь для draft save/load/submit:
  - если заданы `DATABASE_URL` и `WEB_MVP_ASSIGNMENT_ID`, API сохраняет тот же
    answer payload в `interview_runs.answers_json`;
  - если `WEB_MVP_ASSIGNMENT_ID` не задан, API временно использует in-memory
    fallback, пока не реализован вход по ссылке и создание `Assignment`;
  - после `submitted` DB-запись не редактируется через draft save.
- Оставшиеся задачи:
  - заменить fallback на обязательный `InterviewRun` после invitation/auth-flow;
  - добавить миграцию/индекс для быстрого поиска черновика по `profileId` или
    перейти на открытие черновика по `interview_run_id`;
  - убрать локальный `profileId` как главный идентификатор после появления
    `Assignment -> InterviewRun` на входе пользователя.
- Ответы сохраняются в `interview_runs.answers_json`.
- Поддержать статусы:
  - `not_started`;
  - `in_progress`;
  - `submitted`.
- Сохранять:
  - `assignment_id`;
  - `answers_json`;
  - `started_at`;
  - `last_saved_at`;
  - `submitted_at`.
- После `submitted` запретить редактирование анкеты в MVP.
- Добавить API-эндпоинты для draft save/load/submit поверх `interview_runs`,
  а не временного `Map`.
- Продумать миграцию текущего localStorage draft в серверный черновик после
  авторизации пользователя.
- Сохранить localStorage только как offline-safe fallback, а не как основной
  источник данных.

## 3. Пользователи, назначения и роли

- Добавить тестовые `users` с ролями:
  - `employee`;
  - `manager`;
  - `hr`;
  - `moderator`;
  - `admin`.
- Добавить или импортировать тестовые `assignments`.
- Связать анкету не с локальным `profileId`, а с цепочкой:

```text
User -> Assignment -> Position -> OrgUnit -> InterviewRun
```

- Реализовать создание или поиск `User` по корпоративной почте.
- Реализовать создание или поиск активного `Assignment` для пользователя и
  должности.
- Предотвращать дубли активных assignment для пары `user_id + position_id`.
- Добавить минимальную role/permission-модель для доступа к web-экранам.

## 4. Вход по ссылке и старт анкеты

- Добавлено:
  - `GET /api/invitations/:token` для предпросмотра активной ссылки;
  - `POST /api/invitations/:token/start` для старта анкеты;
  - web-экран входа по ссылке `?invite=...` до анкеты;
  - пользователь видит должность и оргструктуру, для которых создано
    приглашение;
  - пользователь вводит корпоративную почту и ФИО;
  - web API находит или создаёт `User`;
  - web API находит или создаёт активный `Assignment`;
  - web API создаёт или открывает `InterviewRun`;
  - после старта анкета использует `profileId = interview_runs.id`, не меняя
    форму answer payload;
  - ссылки ищутся по `token_hash`; raw token не хранится;
  - обработаны недоступные состояния: не найдена, не active, expired,
    max uses reached.
- Оставшиеся задачи:
  - добавить интерфейс модератора/администратора для создания ссылок;
  - добавить copy-link UI и отзыв ссылки;
  - решить политику repeated uses: когда увеличивать `used_count` для
    повторного открытия уже созданного черновика тем же пользователем;
  - добавить полноценные backend-тесты для invitation flow.

## 5. Интерфейс модератора/администратора

- Добавить web-экран поиска и выбора должности для создания invitation link.
- Показывать должность вместе с оргструктурой, чтобы избежать неоднозначности.
- Добавить список созданных ссылок:
  - должность;
  - оргструктура;
  - статус;
  - срок действия;
  - число использований;
  - кто создал.
- Реализовать отзыв ссылки.
- Реализовать копирование ссылки для отправки сотруднику.
- Добавить фильтры по статусу ссылки, оргструктуре и должности.

## 6. Интерфейс сотрудника

- Добавить стартовый экран анкеты после входа по ссылке.
- На стартовом экране показать:
  - ФИО сотрудника;
  - корпоративную почту;
  - выбранную должность;
  - оргструктуру;
  - статус анкеты;
  - кнопку продолжения черновика или начала заполнения.
- Если по ссылке уже есть черновик, открывать продолжение, а не новую анкету.
- Если анкета отправлена, показывать read-only состояние и статус отправки.
- Явно показывать, какие данные пришли из справочника и не редактируются.

## 7. Интерфейс руководителя и HR

- Спроектировать web-интерфейс руководителя для просмотра результатов
  подчинённых.
- Руководитель должен видеть только доступный ему scope оргструктуры.
- Использовать `org_unit_reviewers` для определения доступа.
- Основные экраны:
  - список подчинённых/назначений;
  - статус прохождения анкеты;
  - карточка интервью;
  - ответы сотрудника по разделам;
  - сгенерированный `InterviewProfile`;
  - `ExpectedProfile`;
  - сравнение профилей и найденные расхождения.
- Добавить фильтры:
  - оргструктура;
  - должность;
  - статус интервью;
  - наличие расхождений;
  - требует внимания.
- Для HR/модератора добавить более широкий обзор по подразделениям.
- Пока profile-agent не готов, предусмотреть пустые/ожидающие состояния:
  - профиль ещё не сгенерирован;
  - сравнение ещё не выполнено;
  - документов для expected profile недостаточно.

## 8. Отображение результатов profile-agent в web

Эта часть относится к web-интеграции, не к реализации LLM.

- Добавить API и UI для чтения:
  - `interview_profiles`;
  - `expected_profiles`;
  - `profile_comparisons`.
- Показывать статусы генерации:
  - pending;
  - generated;
  - failed;
  - stale.
- Показывать ошибки генерации в человеко-понятном виде.
- Для сравнения профилей показывать:
  - совпадения;
  - расхождения;
  - конфликты;
  - отсутствующее в интервью;
  - лишнее в интервью;
  - severity;
  - needs_review.
- Дать руководителю/HR возможность открыть исходные ответы, из которых
  получен блок `InterviewProfile`.

## 9. Валидация, тесты и качество UX

- Покрыть тестами текущую механику явной валидации:
  - подсветка конкретного блока;
  - inline-сообщение;
  - сценарий `Да`, но карточек нет;
  - сценарий карточка есть, но обязательные поля пустые.
- Добавить тесты на:
  - conditional field visibility;
  - exclusive multi-choice options;
  - draft save/restore;
  - submitted completeness;
  - directory autocomplete.
- Добавить визуальные проверки для ключевых сценариев анкеты.
- Сохранить правило: если пользователь заблокирован, интерфейс должен явно
  показать где проблема и что нужно сделать.

## 10. Данные справочника и качество выбора

- Продолжить улучшение поиска по справочнику должностей и подразделений.
- Хранить не только человеко-читаемую строку выбранной должности, но и
  стабильные ids:
  - `position_id`;
  - `org_unit_id`.
- Для черновика можно хранить и ids, и display label, чтобы UI быстро
  восстанавливался, но backend должен опираться на ids.
- Продумать миграцию текущих строковых значений к структурированным значениям.
- Для всех autocomplete-полей решить, где нужен выбор только должности, где
  только подразделения, а где смешанный directory search.

## 11. Открытые web/product решения

- Нужно решить, какие поля анкеты остаются свободным текстом, а какие должны
  стать выбором из справочника.
- Нужно решить, как показывать сотруднику уже известные данные из назначения:
  должность, оргструктура, руководитель, подчинённые.
- Нужно решить, какие данные сотрудник может уточнять вручную, а какие являются
  справочными и не редактируются.
- Нужно определить, что видит руководитель до генерации профилей: только ответы
  анкеты или предварительный статус.
- Нужно определить жизненный цикл исправлений после отправки. В MVP
  редактирование после `submitted` запрещено, но может понадобиться workflow
  возврата на доработку.

## 12. Отдельный не-web трек

Эти задачи связаны с web-интерфейсом, но должны жить в отдельном TODO для
profile-agent/LLM:

- Python profile-agent.
- Pydantic-схемы для входа и выходов агента.
- Генерация `InterviewProfile`.
- Генерация `ExpectedProfile` из artifacts.
- Сравнение профилей.
- Prompt contracts.
- Трассировка LLM-вызовов.
- Оценка качества профилей и сравнений.
